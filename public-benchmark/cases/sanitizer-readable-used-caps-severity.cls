public without sharing class B { void m(){ List<Sharing_Test__c> r = [SELECT Customer_IBAN__c FROM Sharing_Test__c]; List<Sharing_Test__c> safe = Security.stripInaccessible(AccessType.READABLE, r).getRecords(); } }
