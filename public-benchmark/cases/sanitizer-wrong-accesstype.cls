public without sharing class B { void m(){ List<Blast_Test__c> r = [SELECT Customer_IBAN__c FROM Blast_Test__c]; List<Blast_Test__c> s = Security.stripInaccessible(AccessType.UPDATABLE, r).getRecords(); } }
